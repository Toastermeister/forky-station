<DefaultWindow xmlns="https://spacestation14.io"
               xmlns:gfx="clr-namespace:Robust.Client.Graphics;assembly=Robust.Client"
               Title="{Loc 'bomb-defusal-title'}"
               MinSize="500 400"
               SetSize="600 500">
    <BoxContainer Orientation="Vertical"
                  HorizontalExpand="True"
                  VerticalExpand="True">
        <!-- Header: Serial, Strikes, Timer -->
        <PanelContainer Margin="0 0 0 5">
            <PanelContainer.PanelOverride>
                <gfx:StyleBoxFlat BackgroundColor="#1a1a2e" />
            </PanelContainer.PanelOverride>
            <BoxContainer Orientation="Horizontal" Margin="8 4">
                <Label Name="SerialLabel"
                       Text="Serial: ------"
                       FontColorOverride="#00ff41"
                       HorizontalExpand="True" />
                <Label Name="StrikesLabel"
                       Text="Strikes: 0/3"
                       FontColorOverride="#ff4444"
                       Margin="10 0" />
                <Label Name="TimerLabel"
                       Text="Time: --:--"
                       FontColorOverride="#ffaa00"
                       HorizontalAlignment="Right" />
            </BoxContainer>
        </PanelContainer>
        <!-- Status label -->
        <PanelContainer Name="StatusPanel" Margin="0 0 0 5" Visible="False">
            <PanelContainer.PanelOverride>
                <gfx:StyleBoxFlat BackgroundColor="#003300" />
            </PanelContainer.PanelOverride>
            <Label Name="StatusLabel"
                   Text=""
                   FontColorOverride="#00ff41"
                   Align="Center" />
        </PanelContainer>
        <!-- Module Grid -->
        <GridContainer Name="ModuleGrid"
                       Columns="2"
                       HorizontalExpand="True"
                       VerticalExpand="True" />
    </BoxContainer>
</DefaultWindow>
